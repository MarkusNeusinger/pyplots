library: seaborn
language: python
specification_id: network-force-directed
created: '2025-12-23T15:15:25Z'
updated: '2026-04-26T09:00:45Z'
generated_by: claude-opus
workflow_run: 24952470496
issue: 990
python_version: 3.14.4
library_version: 0.13.2
preview_url_light: https://storage.googleapis.com/anyplot-images/plots/network-force-directed/python/seaborn/plot-light.png
preview_url_dark: https://storage.googleapis.com/anyplot-images/plots/network-force-directed/python/seaborn/plot-dark.png
preview_html_light: null
preview_html_dark: null
quality_score: 76
review:
  strengths:
  - Force-directed layout algorithm (Fruchterman-Reingold) implemented inline without
    networkx dependency — works correctly
  - Realistic corporate network scenario (Engineering/Marketing/Sales departments)
    with plausible intra/inter-community connection probabilities
  - 'All spec features present: degree-scaled node sizes, weight-scaled edge thickness,
    hub node labels, 150 iterations for stabilization'
  - Node labels restricted to high-degree hubs (75th percentile threshold) — avoids
    clutter while surfacing key actors
  - np.random.seed(42) ensures reproducibility; KISS flat script structure with no
    functions or classes
  weaknesses:
  - Code saves as plot.png with facecolor='white' and no ANYPLOT_THEME env var — must
    read os.getenv('ANYPLOT_THEME', 'light') and save as plot-{THEME}.png with correct
    PAGE_BG, INK, INK_SOFT tokens
  - 'Palette uses [''#306998'', ''#FFD43B'', ''#E74C3C''] — forbidden Python Blue
    and arbitrary custom hexes; must use Okabe-Ito: [''#009E73'', ''#D55E00'', ''#0072B2'']'
  - Title string uses 'pyplots.ai' instead of 'anyplot.ai'; also has extra 'Organizational
    Network · ' prefix — required format is 'network-force-directed · seaborn · anyplot.ai'
  - 'All 4 spines kept (set to #CCCCCC); legend has shadow=True and fancybox=True
    — remove top/right spines, use frameon=True without shadow for a cleaner look'
  - Hub annotation labels use hardcoded color='#333333' (near-black) — dark-on-dark
    failure in dark theme; must use INK token
  - Stats text uses hardcoded color='#666666' — also not theme-adaptive; must use
    INK_MUTED or INK_SOFT token
  - Hub label fontsize=12 is below the 16pt minimum for secondary text at 4800x2700px
    canvas
  - seaborn contribution is only sns.scatterplot() — LM-02 score low; consider using
    sns.set_theme() rc dict with full theme-adaptive chrome as a distinctive seaborn
    pattern
  image_description: |-
    Light render (plot-light.png):
      Background: Warm off-white (~#FAF8F1), not pure white — correct for light theme.
      Chrome: Title "Organizational Network · network-force-directed · seaborn · anyplot.ai" in dark text, clearly readable. Axis labels "Force-Directed X Position" and "Force-Directed Y Position" in dark text, readable. No tick values (removed — appropriate for network viz). Legend ("Department") in upper-left with department labels readable.
      Data: Three communities clearly distinguished: Engineering (teal/green ~#009E73, 15 nodes, left cluster), Marketing (orange/vermillion ~#D55E00, 12 nodes, upper-right cluster), Sales (blue ~#0072B2, 10 nodes, scattered outer). Node sizes vary by degree (hub nodes noticeably larger). Edges in light gray, varying thickness by weight. Hub node labels (Node 3, Node 4, Node 21, Node 20, Node 28, Node 30) positioned above respective nodes. Network stats annotation visible at bottom center.
      Legibility verdict: PASS — all chrome text readable; minor note that hub labels are small (~12pt).

    Dark render (plot-dark.png):
      Background: Warm near-black (~#1A1A17), not pure black — correct for dark theme.
      Chrome: Title, axis labels, legend all rendered in light text (white/off-white), clearly readable against dark background. No dark-on-dark failure visible in the images (note: the code in seaborn.py uses hardcoded color='#333333' for hub annotations and '#666666' for stats text — these would be dark-on-dark failures if the images truly came from that code, suggesting a regenerated code version produced these images).
      Data: Identical Okabe-Ito data colors as light render (Engineering teal, Marketing orange, Sales blue) — correct, only chrome flips. Edges appear slightly lighter on dark background. All node and community structure identical.
      Legibility verdict: PASS (images show readable text) — however, the source code has hardcoded dark annotation colors that would fail dark theme if executed as written.
  criteria_checklist:
    visual_quality:
      score: 26
      max: 30
      items:
      - id: VQ-01
        name: Text Legibility
        score: 6
        max: 8
        passed: true
        comment: Title 24pt, axis labels 20pt, legend 16pt — all explicit. Hub annotation
          labels at fontsize=12 are below the 16pt minimum for secondary/tertiary
          text at 4800x2700px canvas.
      - id: VQ-02
        name: No Overlap
        score: 6
        max: 6
        passed: true
        comment: Force-directed layout distributes nodes well; hub labels positioned
          above nodes with offset, no collisions visible.
      - id: VQ-03
        name: Element Visibility
        score: 5
        max: 6
        passed: true
        comment: Node sizes scale by degree (150 + degree*60), edges vary by weight
          (linewidth 1+weight*2). Visibility good; node size range is somewhat subtle.
      - id: VQ-04
        name: Color Accessibility
        score: 2
        max: 2
        passed: true
        comment: Okabe-Ito palette is CVD-safe. Three distinct communities clearly
          distinguishable without relying on hue alone (size also differs).
      - id: VQ-05
        name: Layout & Canvas
        score: 3
        max: 4
        passed: true
        comment: 'Network fills most of canvas with good balance. Legend placed upper-left,
          stats annotation at bottom. Minor: axis labels on a network viz with no
          tick values add minimal value.'
      - id: VQ-06
        name: Axis Labels & Title
        score: 2
        max: 2
        passed: true
        comment: Axis labels are descriptive ('Force-Directed X/Y Position'). Title
          present and readable.
      - id: VQ-07
        name: Palette Compliance
        score: 2
        max: 2
        passed: true
        comment: 'Images show Okabe-Ito colors (Engineering=#009E73, Marketing=#D55E00,
          Sales=#0072B2), warm off-white light background, warm near-black dark background,
          theme-adaptive chrome. NOTE: source code uses wrong palette (#306998/#FFD43B/#E74C3C)
          and no theme adaptation — images appear to be from a regenerated version.'
    design_excellence:
      score: 8
      max: 20
      items:
      - id: DE-01
        name: Aesthetic Sophistication
        score: 4
        max: 8
        passed: true
        comment: Well-configured but not exceptional. Clear community separation,
          degree-scaled nodes, hub labels add value. Standard seaborn scatter aesthetics.
      - id: DE-02
        name: Visual Refinement
        score: 2
        max: 6
        passed: false
        comment: 'All 4 spines kept (colored #CCCCCC instead of removed). Legend has
          shadow=True and fancybox=True adding unnecessary decoration. sns.set_theme(style=''white'')
          rather than the recommended ''ticks'' with full rc params.'
      - id: DE-03
        name: Data Storytelling
        score: 2
        max: 6
        passed: false
        comment: Community structure is visible but there is no guided narrative.
          Hub nodes are labeled but no emphasis on inter-community bridges or most
          central node. Data is displayed, not interpreted.
    spec_compliance:
      score: 14
      max: 15
      items:
      - id: SC-01
        name: Plot Type
        score: 5
        max: 5
        passed: true
        comment: Correct force-directed graph with Fruchterman-Reingold algorithm
          implemented inline. Physics simulation with repulsive+attractive forces
          and simulated annealing cooling.
      - id: SC-02
        name: Required Features
        score: 4
        max: 4
        passed: true
        comment: Node size scales by degree. Edge thickness scales by weight. 150
          iterations for layout stabilization. Node labels for high-degree hubs (75th
          percentile threshold).
      - id: SC-03
        name: Data Mapping
        score: 3
        max: 3
        passed: true
        comment: Nodes positioned by force-directed algorithm. Communities color-coded.
          Edges represent connections. Node sizes represent degree centrality.
      - id: SC-04
        name: Title & Legend
        score: 2
        max: 3
        passed: false
        comment: Images show title 'Organizational Network · network-force-directed
          · seaborn · anyplot.ai' — extra 'Organizational Network · ' prefix violates
          required format '{spec-id} · {library} · anyplot.ai'. Code also uses 'pyplots.ai'
          (wrong brand). Legend labels (Engineering/Marketing/Sales with counts) are
          correct.
    data_quality:
      score: 15
      max: 15
      items:
      - id: DQ-01
        name: Feature Coverage
        score: 6
        max: 6
        passed: true
        comment: 'Shows all aspects: community structure (3 departments), hub-and-spoke
          topology, inter-community bridges, degree variation, weighted edges. Intra
          (35%) vs inter (5%) connection probability creates clear community separation.'
      - id: DQ-02
        name: Realistic Context
        score: 5
        max: 5
        passed: true
        comment: Corporate organizational network (Engineering 15, Marketing 12, Sales
          10) is a real-world neutral scenario. Plausible collaboration patterns within
          and across departments.
      - id: DQ-03
        name: Appropriate Scale
        score: 4
        max: 4
        passed: true
        comment: 37 nodes is in the ideal 20-200 range. Intra-community 35% and inter-community
          5% connection probability produces realistic network density. Edge weights
          0.3-1.0 appropriate.
    code_quality:
      score: 9
      max: 10
      items:
      - id: CQ-01
        name: KISS Structure
        score: 3
        max: 3
        passed: true
        comment: 'Flat script: imports → seed → data generation → FD algorithm → plotting
          → save. No functions or classes.'
      - id: CQ-02
        name: Reproducibility
        score: 2
        max: 2
        passed: true
        comment: np.random.seed(42) set before all random operations.
      - id: CQ-03
        name: Clean Imports
        score: 2
        max: 2
        passed: true
        comment: Only matplotlib.pyplot, numpy, and seaborn imported — all used.
      - id: CQ-04
        name: Code Elegance
        score: 2
        max: 2
        passed: true
        comment: FD algorithm implemented as nested loops — verbose but appropriate
          for avoiding networkx dependency. Custom legend via plt.scatter([],[]) is
          standard workaround. Overall code is clean.
      - id: CQ-05
        name: Output & API
        score: 0
        max: 1
        passed: false
        comment: Saves as 'plot.png' with facecolor='white'. Must save as 'plot-{THEME}.png'
          where THEME=os.getenv('ANYPLOT_THEME','light'). No theme env var read, no
          PAGE_BG/INK token setup.
    library_mastery:
      score: 4
      max: 10
      items:
      - id: LM-01
        name: Idiomatic Usage
        score: 3
        max: 5
        passed: true
        comment: sns.scatterplot() with hue, size, sizes, edgecolor, linewidth is
          idiomatic seaborn. However, the majority of the code (FD algorithm, edge
          drawing) uses pure numpy/matplotlib — seaborn is only the final scatter
          renderer.
      - id: LM-02
        name: Distinctive Features
        score: 1
        max: 5
        passed: false
        comment: The seaborn contribution is essentially a styled scatter plot — could
          be replicated with plt.scatter() with minor changes. No use of FacetGrid,
          PairGrid, or seaborn-specific statistical features. The sns.set_theme()
          rc dict pattern (from the seaborn.md guide) is also not used.
  verdict: REJECTED
impl_tags:
  dependencies: []
  techniques:
  - annotations
  - custom-legend
  - manual-ticks
  patterns:
  - data-generation
  - iteration-over-groups
  - explicit-figure
  dataprep:
  - normalization
  styling:
  - alpha-blending
  - edge-highlighting
